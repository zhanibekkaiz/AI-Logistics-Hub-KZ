# 🤖 Настройка Telegram Bot

## 📋 Пошаговая инструкция

### 1. Создание бота в Telegram

1. **Откройте Telegram** и найдите @BotFather
2. **Отправьте команду** `/newbot`
3. **Введите имя бота** (например: "AI Logistics Hub")
4. **Введите username бота** (например: "ailogistics_hub_bot")
5. **Сохраните токен** - он понадобится для настройки

### 2. Настройка переменных окружения

Добавьте в файл `.env`:

```env
# Telegram Bot настройки
TELEGRAM_BOT_TOKEN=your_bot_token_here
```

### 3. Настройка вебхука

После запуска сервера, установите вебхук:

```bash
# Замените на ваш домен и токен
curl -X POST "https://api.telegram.org/botYOUR_BOT_TOKEN/setWebhook" \
     -H "Content-Type: application/json" \
     -d '{"url": "https://your-domain.com/api/v1/telegram/webhook"}'
```

### 4. Проверка работы бота

1. **Найдите вашего бота** в Telegram по username
2. **Отправьте команду** `/start`
3. **Проверьте функциональность** всех кнопок

## 🔧 Функции бота

### 📦 Расчет доставки
- Принимает данные о грузе (вес, объем, маршрут)
- Рассчитывает стоимость карго и белой доставки
- Сохраняет расчеты в Airtable

### 🔍 Поиск ТН ВЭД
- Принимает описание товара
- Находит подходящий ТН ВЭД код
- Показывает дополнительную информацию

### 🏢 Проверка поставщика
- Принимает название китайской компании
- Показывает предварительную оценку надежности
- Дает рекомендации

### 📊 История расчетов
- Показывает последние расчеты пользователя
- Сохраняет все данные в Airtable

## 🚀 Запуск и тестирование

### Локальный запуск
```bash
# Запустите сервер
uvicorn app.main:app --reload

# Установите вебхук для локального тестирования (ngrok)
curl -X POST "https://api.telegram.org/botYOUR_BOT_TOKEN/setWebhook" \
     -H "Content-Type: application/json" \
     -d '{"url": "https://your-ngrok-url.ngrok.io/api/v1/telegram/webhook"}'
```

### Продакшн запуск
```bash
# Установите вебхук на продакшн домен
curl -X POST "https://api.telegram.org/botYOUR_BOT_TOKEN/setWebhook" \
     -H "Content-Type: application/json" \
     -d '{"url": "https://ailogistics.kz/api/v1/telegram/webhook"}'
```

## 📱 Интерфейс бота

### Главное меню
```
📦 Расчет доставки  🔍 Поиск ТН ВЭД
🏢 Проверка поставщика  📊 Мои расчеты
ℹ️ Помощь  📞 Связаться с менеджером
```

### Пример расчета
```
Вес: 100
Объем: 0.5
Откуда: Shenzhen
Куда: Almaty
Товар: LED лампы, 10W, E27
```

## ⚠️ Важные замечания

1. **Токен бота** должен быть секретным
2. **Вебхук URL** должен быть HTTPS
3. **Порт 443** должен быть открыт для вебхука
4. **Логирование** всех сообщений включено

## 🔍 Отладка

### Проверка статуса вебхука
```bash
curl "https://api.telegram.org/botYOUR_BOT_TOKEN/getWebhookInfo"
```

### Удаление вебхука
```bash
curl -X POST "https://api.telegram.org/botYOUR_BOT_TOKEN/deleteWebhook"
```

### Просмотр логов
```bash
# Логи бота сохраняются в структурированном формате
tail -f logs/app.log | grep telegram
```

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи сервера
2. Убедитесь, что вебхук установлен правильно
3. Проверьте доступность API Telegram
4. Обратитесь к документации Telegram Bot API
